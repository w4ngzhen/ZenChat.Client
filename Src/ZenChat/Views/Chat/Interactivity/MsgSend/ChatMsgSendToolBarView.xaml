<UserControl x:Class="ZenChat.Views.Chat.Interactivity.MsgSend.ChatMsgSendToolBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZenChat.Views.Chat.Interactivity.MsgSend"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="500">
    <Border BorderThickness="0">
        <Grid>
            <ListBox x:Name="ChatMsgSendToolItems" 
                     Style="{StaticResource ChatMsgSendToolBarStyle}"
            >
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </Grid>
    </Border>
</UserControl>
